(ns alfa.applogic.user)

(defn add-user!
  "Add user... duh!!"
  [])

(defn set-user!
  "To change user profile"
  [])

(defn set-password!
  "To change user password"
  [])

(defn valid-user?
  "Probably for login stuff"
  [])